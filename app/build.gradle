plugins {
    id 'com.android.application' version '8.2.0'
}

android {
    namespace 'com.oppo.spoof'
    compileSdk 35

    defaultConfig {
        applicationId 'com.cmyf.oppo.spoof'
        minSdk 26
        targetSdk 35
        versionCode 23
        versionName 'v2.3'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    // LSPosed Xposed API（provided，不打包进 APK）
    compileOnly 'de.robv.android.xposed:api:82'
}
